## structure 
#   ## data pipeline is separated into internal and external
#           >> internal: LP-USA only (e.g. user contrib metrics) 
#           >> external: sourced data (e.g. GIS data) 
#   ## external GIS datapipelines 
#       >> has 4 folders in main/root directory 
#       >> each folder is for each step in data pipeline proc

#   ## FOLDER STRUC
#       LIVE/NON-TRIAL: EXTERNAL/GIS/<pipeline proc>/<state>/<county> 
#       TRIAL: EXTERNAL/GIS/<trial>/<pipeline proc>/<state>/<county> 
#   #
